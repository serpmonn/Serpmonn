# Article Generator configuration
PROVIDER_TYPE=mock
LANGUAGES=ru,en,zh
STORAGE_DIR=/app/data
GENERATOR_INTERVAL_SECONDS=900
MAX_ARTICLES=1000

# OpenAI provider settings (if PROVIDER_TYPE=openai)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Ollama provider settings (if PROVIDER_TYPE=ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Server
HOST=0.0.0.0
PORT=8080
---
pagination:
  data: locales
  size: 1
  alias: locale
permalink: "{% if locale == 'ru' %}/frontend/register/register.html{% else %}/frontend/{{ locale }}/register/register.html{% endif %}"
eleventyComputed:
  authUrl: "{% if locale == 'ru' %}/frontend/auth/auth.html{% else %}/frontend/{{ locale }}/auth/auth.html{% endif %}"
---

<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirecting…</title>
    <link rel="canonical" href="{{ authUrl }}?tab=register">
    <script>
        (function () {
            var params = new URLSearchParams(window.location.search);
            params.set('tab', 'register');
            var target = '{{ authUrl }}?' + params.toString();
            window.location.replace(target);
        })();
    </script>
    <noscript>
        <meta http-equiv="refresh" content="0;url={{ authUrl }}?tab=register">
    </noscript>
</head>
<body>
    <p><a href="{{ authUrl }}?tab=register">Continue to registration</a></p>
</body>
</html>
